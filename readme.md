# Spring Cloud Aws S3 Service

## Quick start

<hr />

- resource in the blanks in application yml
- resource in the blanks in application prod yml

### start with gradle

``
./gradlew bootRun
``

### start with docker

```
./gradlew bootJar
docker-compose up --build -d
```